using System;
using WebService;

namespace Portfolio_Subproject2
{
    class Program
    {
        static void Main(string[] args)
        {
            WebService.WebService service = new WebService.WebService(args);
        }
    }
}
